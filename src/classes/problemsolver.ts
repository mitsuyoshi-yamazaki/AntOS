import { ScopeInterface } from './scope';

class PloblemSolver implements ScopeInterface {
  constructor (name: string, owned_controllers: StructureController[]) {
    console.log(`PloblemSolver.new not implemented yet`)
  }

  run(): void {
    console.log(`PloblemSolver.run not implemented yet`)
  }
}
